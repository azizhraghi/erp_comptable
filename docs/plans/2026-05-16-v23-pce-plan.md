# Implementation Plan: V23 - Module Plan Comptable (PCE)

## Overview
This plan details the steps to implement the Plan Comptable module in the ComptaExpert ERP.

## Proposed Changes
### HTML/JS (COMPTAEXPERT_V23.html)
- Add `page-pce` container to the main content area.
- Implement `renderPCE()` function.
- Implement account creation/edition logic.
- Add preloaded SYSCOHADA account data to the initial state.

## Step-by-Step Instructions

### 1. File Preparation
- [ ] Duplicate `COMPTAEXPERT_V22.html` to `COMPTAEXPERT_V23.html`.
- [ ] Update version number in the title and login screen.
- **Verification**: Check if V23 file opens correctly.

### 2. State & Data
- [ ] Initialize `state.pce` with a few sample accounts if empty.
- [ ] Update `exportData` and `drop` logic to handle the new `pce` field (already covered by JSON stringify/parse).
- **Verification**: `console.log(state.pce)` in dev tools.

### 3. UI Structure
- [ ] Add `<div id="page-pce" class="hidden animate-fade-up">` to `#page-container`.
- [ ] Add header with "Plan Comptable" title, Search input, and "Ajouter un compte" button.
- [ ] Add a table structure for account listing.
- **Verification**: Click "Plan Comptable" in sidebar and see the empty table/header.

### 4. Account Rendering
- [ ] Implement `renderPCE()` to populate the table with filtering logic.
- [ ] Add badges for Classes (1-7) and Types (Actif/Passif/etc.).
- **Verification**: See sample accounts in the table.

### 5. Account Management (Modals)
- [ ] Implement `openAccountModal(id)` to add or edit accounts.
- [ ] Implement `saveAccount()` with validation (unique number).
- [ ] Implement `deleteAccount(id)` with safety check (check if used in `ecritures`).
- **Verification**: Add a new account and see it in the list.

### 6. Search & Filters
- [ ] Add instant search logic by number or libellé.
- [ ] Add Class filter (Class 1, Class 2, etc.).
- **Verification**: Search for "411" and see "Clients".

## Final Verification
- [ ] Verify that adding an account persists after export/import.
- [ ] Verify that deleting an account works correctly.
- [ ] Check UI consistency with V22 style.
